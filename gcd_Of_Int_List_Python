def generalizedGCD(num, arr):
    print(arr)
    def gcd(a, b):
        while b > 0:
            a, b = b, a % b
        return a

    a = arr

    result = a[0]
    for i in a[1:]:
        print(a[1:])
        result = gcd(result, i)
    return(result)
